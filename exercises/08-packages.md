# Chapter 8 Exercises — Packages

[← Chapter](../book/08-packages.md) · [Solutions →](../solutions/08-packages.md)

1. Split a one-file calculator into `app`, `expression`, and `evaluation` packages.
2. Explain why package directories, not files, are the compilation unit.
3. Configure a `project:` collection and import a local package through it.
4. Rename a vague `utils` package into two coherent capabilities.
5. Draw the dependency graph for an app, renderer, assets, platform, and math packages.
6. Resolve a conceptual cycle between `game` and `renderer`.
7. Design a narrow public API for an asset registry.
8. Localize Windows, Linux, and macOS implementation details behind one platform package.
9. Explain what belongs in the `main` composition root.
10. Challenge: propose a package layout for a small 2D game editor and justify every dependency direction.
