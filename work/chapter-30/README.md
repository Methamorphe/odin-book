# Chapter 30 Workshop — Resource Handle Console

Commands: `create <name>`, `destroy <index> <generation>`, and `get <index> <generation>`.

Handles contain index plus generation; destroyed slots are safely reusable.
