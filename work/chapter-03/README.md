# Chapter 03 Workshop — Interactive Greeter

Commands: `greet <name>`, `style plain|loud`, plus the shared shell commands.

```bash
odin run work/chapter-03
odin test work/chapter-03
```

The process remains alive until `quit`.
