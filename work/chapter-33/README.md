# Chapter 33 Workshop — Asset Registry Shell

Commands: `load <path>`, `retain <handle>`, `release <handle>`, and `reload <handle>`.

Track normalized keys, generations, states, references, and artifact metadata.
