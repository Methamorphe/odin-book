# Chapter 17 Workshop — Directory Inspector

Commands: `list <path>` and `filter <extension>`.

Collect owned metadata, sort deterministically, continue after recoverable entry errors, and summarize failures.
