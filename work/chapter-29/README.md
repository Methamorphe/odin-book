# Chapter 29 Workshop — Particle Layout Experiment

Commands: `layout aos|soa` and `bench <count> <iterations>`.

Both layouts initialize from the same deterministic seed and produce equal logical state.
