# Chapter 09 Workshop — Scoreboard Editor

Commands: `add <name> <score>`, `remove <index>`, `list`, `average`, and shared shell commands.

Start with fixed storage, then migrate to a dynamic array while observing capacity changes.
